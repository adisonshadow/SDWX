# 第一阶段实施计划

> 本文档为已批准计划的归档副本 + WBS 排期，供实施跟踪。

---

## 决策摘要

| 项 | 值 |
|---|---|
| EADAF 地址 / 应用 | `http://localhost:9527/` / code `FMMS` / id `10000000-0001-4000-8000-000000000006` |
| 工位范围 | 全部 5 种 |
| PCS 对接 | Mock（工卡种子放 EADAF） |
| 双屏 | 第一阶段含双屏 |
| 前端版本 | React 19 + antd 6 + RR7 + Vite 8 |
| 采集 | Electron 原始转发 + BFF 结构化 → EADAF ingest |
| 后端 | admin/backend 无 DB 纯 BFF |
| 不做 | /admin、通知客户端、监控大屏、真实 PCS/AGV/立库 |

---

## WBS 排期

### 阶段 0 — 工程地基 ✅
- [x] monorepo（pnpm workspaces + shared 包 + tsconfig/eslint/prettier）
- [ ] 三应用脚手架（backend Express / frontend Vite / Electron）
- [ ] 复用参考工程 auth/api 代码迁入 frontend
- [x] 写 6 份功能文档

### 阶段 1 — EADAF 对齐
- [ ] EADAF 建模（按 `EADAF数据结构设计-FMMS.md` §4 §5：9 新实体 + 3 扩展 + 采集管道）— **用户在 EADAF 操作**
- [ ] BFF EADAFClient（token 缓存/刷新/重试）
- [ ] BFF repositories 适配层（routePath 常量化）
- [ ] Mock 种子数据脚本（写 EADAF）

### 阶段 2 — 通用工位主线（主屏）
- [ ] SSO 全链路（frontend /auth + BFF /auth + Electron 透传）
- [ ] 工位选择 + 持久化（frontend + Electron store）
- [ ] 工卡列表/详情/接受/进度 + WS 工卡推送（BFF mockPCS → WS）
- [ ] 零部件看板 + 扫码定位

### 阶段 3 — 副屏业务（5 工位）
- [ ] 双屏架构（Electron 双窗 + BroadcastChannel + OPShell）
- [ ] 分拣副屏（形迹码盘 + 齐套校验）
- [ ] 故检副屏（检测表 + 自动采集/判异 + 拍照）+ 采集链路
- [ ] 修理副屏（修理记录/委外拆单打印/报废/处置完成校验）
- [ ] 收发副屏（线边库/出入库/委外收发）
- [ ] 通用：异常呼叫、工艺视频内播放、标签打印

### 阶段 4 — 采集与工具
- [ ] Electron 采集服务（serial + USB HID 驱动、注册、状态监控、报警）
- [ ] 测量工具档案（读 EADAF）+ 状态监控面板
- [ ] 手动输入/判异覆盖 + 检测图片（EADAF 文件存储）

### 阶段 5 — 联调收尾
- [ ] Mock PCS 工卡循环（下发→执行→上报回写 EADAF）
- [ ] 双屏端到端联调
- [ ] 部署联调手册完善
- [ ] P1 功能验收对照

---

## 早期 Demo 闭环
登录 → 选故检工位 → Mock 工卡 → 副屏采集一条数据写 EADAF。
（阶段 2 + 阶段 3 故检 + 阶段 4 采集 的子集，优先打通）

---

## 风险登记
- EADAF `applications/token` bootstrap token 问题
- §4/§5 字段命名以 EADAF 实际建模为准（代码 repository 适配）
- 业务 API 写操作（create 已支持；updateOne 单字段语义注意批量）
- SSO 登出流程未文档化
- 采集管道 allowlist
- AGV/立库 P2；re-inspection 循环 Mock 自闭环
- 标签打印 P1 前端方案，标签机驱动视现场
